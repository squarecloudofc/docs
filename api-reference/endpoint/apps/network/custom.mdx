---
title: "Custom Domain Integration"
sidebarTitle: "Custom Domain"
description: "Integrate your website with a custom domain."
api: "POST https:/api.squarecloud.app/v2/apps/{app_id}/network/custom/{custom_domain}"
authMethod: "none"
---

**Is necessary to have at least the [senior plan](https://squarecloud.app/plans) to use this route.**

<ParamField header="Authorization" type="string" placeholder="API KEY" required>
  The API key for your account. You can find this in your [account settings](https://squarecloud.app/dashboard/account).
</ParamField>

<ParamField path="app_id" type="string" placeholder="Application ID" required>
  The ID of the application. You can find this in the URL of your application's dashboard.
</ParamField>

<ParamField path="custom_domain" type="string" placeholder="example.com" required>
  The custom domain you want to use.
</ParamField>

### Response

<ResponseField name="status" type="string">
  Indicates whether the call was successful. "success" if successful, "error" if not.
</ResponseField>

<ResponseField name="response" type="object">

The contents of the response.

<Expandable title="Toggle object">

</Expandable>
</ResponseField>

<ResponseExample>
```json
{
    "status": "success"
}
```
</ResponseExample>